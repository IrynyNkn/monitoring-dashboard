import type { ThemeConfig } from 'antd';
import { purple } from '@ant-design/colors';

const config: ThemeConfig = {
  token: {
    colorPrimary: purple[5],
    colorBgSpotlight: purple[4],
    colorLink: purple[7],
  }
};

export default config;